/**
 * Clamps `num` within the inclusive range specified by the boundary values `lower` and `upper`.
 * @param num
 * @param lower
 * @param upper
 * @returns
 */
declare const clamp: (num: number, lower: number, upper: number) => number;
export default clamp;
