/**
 * Sorts an array of objects, ordered by properties and orders.
 * @param arr
 * @param props
 * @param orders
 * @returns
 */
declare const orderBy: (arr: any[], props: string[], orders: string[]) => any[];
export default orderBy;
