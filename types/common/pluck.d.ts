/**
 * Converts an array of objects into an array of values corresponding to the specified key.
 * @param arr
 * @param key
 */
declare const pluck: (arr: any[], key: any) => any[];
export default pluck;
