/**
 * Check whether the user has reached the bottom of the specific element.
 * @param el
 * @param offset
 * @returns
 */
declare const isElBottomVisible: (el: any, offset?: number) => boolean;
export default isElBottomVisible;
