/**
 * Check whether the user has reached the bottom of the page.
 *
 * @param {number} [offset=0]
 * @returns {boolean}
 */
declare const isBottomVisible: (offset?: number) => boolean;
export default isBottomVisible;
