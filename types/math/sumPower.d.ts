/**
 * Calculates the sum of the powers of all the numbers from start to end (both inclusive).
 * @param end
 * @param power
 * @param start
 */
declare const sumPower: (end: number, power?: number, start?: number) => number;
export default sumPower;
