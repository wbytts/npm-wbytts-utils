/**
 * Converts a CSV string to a 2D array of objects, the first row of the string is used as the title row.
 *
 * @param {string} data
 * @param {string} [delimiter=',']
 * @returns
 */
declare const CSVToJSON: (data: string, delimiter?: string) => {}[];
export default CSVToJSON;
